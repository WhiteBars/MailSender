<UserControl x:Class="MailSender.View.CustomTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MailSender.View"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" Height="22" Width="Auto"
             x:Name="customControl">
    <DockPanel>
        <TextBlock Text="{Binding ElementName=customControl, Path=Text}"
                       VerticalAlignment="Center" 
                       Margin="0,0,3,0"/>
        <ComboBox Width="150" SelectedIndex="0"
                      ItemsSource="{Binding ElementName=customControl, Path=ItemsSource}">
            <ComboBox.ItemTemplate>
                <DataTemplate DataType="spam:Sender">
                    <StackPanel>
                        <TextBlock>
                               <TextBlock Text="{Binding Name}"/>
                            </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button ToolTip="Добавить отправителя" Click="Button_Click">
            <StackPanel Orientation="Horizontal">
                <fa:ImageAwesome Icon="PlusCircle" Foreground="LimeGreen" Height="16"/>
            </StackPanel>
        </Button>
        <Button ToolTip="Удалить отправителя" Click="Button_Click_1">
            <fa:ImageAwesome Icon="MinusCircle" Foreground="IndianRed" Height="16"/>
        </Button>
        <Button ToolTip="Редактировать" Click="Button_Click_2">
            <fa:ImageAwesome Icon="Edit" Height="16"/>
        </Button>
    </DockPanel>
</UserControl>
